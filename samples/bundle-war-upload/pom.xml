<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.ibm.cics</groupId>
    <artifactId>demo-war-upload</artifactId>
    <version>1.0.0</version>
    <packaging>war</packaging>

    <name>CICS Bundle Maven WAR Upload Sample</name>
    <description>Sample demonstrating direct WAR upload to Liberty server</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
    </properties>

    <dependencies>
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <version>6.0.0</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
            <version>2.1.1</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.ibm.cics</groupId>
            <artifactId>com.ibm.cics.server</artifactId>
            <version>1.800.1-5.6-PH34012</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.4.0</version>
                <configuration>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                </configuration>
            </plugin>

            <!-- CICS Bundle Maven Plugin for WAR Upload -->
            <plugin>
                <groupId>com.ibm.cics</groupId>
                <artifactId>cics-bundle-maven-plugin</artifactId>
                <version>2.0.1-SNAPSHOT</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>upload-war</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <libertyWarUpload>
                        <serverUrl>https://your-server:port/com.ibm.cics.wlp.appdeploy/uploadApp</serverUrl>
                        <!-- Option 1: inline Liberty application XML -->
                        
                        <applicationXml><![CDATA[
                            <application id="demo-war-upload" location="demo-war-upload.war" type="war">
                                <context-root>/demo-war-upload</context-root>
                                <classloader delegation="parentLast"/>
                                <application-bnd>
                                    <security-role name="User">
                                        <user name="IN00501"/>
                                    </security-role>
                                </application-bnd>
                            </application>
                        ]]></applicationXml>
                        
                        <!-- Option 2: read Liberty application XML from a file -->
                        <!-- <applicationXmlLocation>${project.basedir}/src/main/resources/application.xml</applicationXmlLocation> -->
                        <!-- Supply credentials from Maven properties or settings.xml -->
                        <bearerToken>${cics.token}</bearerToken>
                        <!-- Alternative: Basic Authentication -->
                        <userName>${cics.user}</userName>
                        <password>${cics.password}</password>

                    </libertyWarUpload>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
